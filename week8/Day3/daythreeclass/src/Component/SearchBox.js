import React from "react";

const SearchBox = (props) =>{

    return(
        <div>
       <input type={Text} onChange={(e)=> props.sort(e)} placeholder="dude"/>
       </div>
    )

}

export default SearchBox




